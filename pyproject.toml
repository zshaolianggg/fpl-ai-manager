[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fpl-ai-manager"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["openai>=1.40.0", "requests>=2.32.0", "python-dotenv>=1.0.1"]

[tool.setuptools.packages.find]
where = ["src"]
